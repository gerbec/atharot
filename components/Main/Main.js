import styled from 'styled-components';
import {Container, Title1} from '../index';

const Content = styled.div``;

class Main extends React.Component {
    constructor(props) {
        super(props);
        this.state = {};
    }

    render() {
        return <Container></Container>;
    }
}

export default Main;
